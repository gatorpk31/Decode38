// Google Analytics bootstrap — external file so the CSP can drop
// script-src 'unsafe-inline' on a page that renders self-reported medical data.
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
gtag('js', new Date());
gtag('config', 'G-WCPS4QH245');
